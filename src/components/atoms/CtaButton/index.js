/**
 * CtaButton Component
 * 
 * A professional call-to-action button component with:
 * - Multiple variants (primary, secondary, outline, ghost)
 * - Three sizes (small, medium, large) 
 * - Loading and disabled states
 * - Support for both button and link elements
 * - Advanced animations (ripple effect, hover transitions)
 * - Full accessibility support (ARIA attributes, focus management)
 * - Performance optimizations with React.memo
 * - Reduced motion and high contrast support
 * 
 * @see {@link ./CtaButton.jsx} for component implementation
 * @see {@link ./CtaButton.styled.js} for styling and animations
 */
export { default } from './CtaButton';
