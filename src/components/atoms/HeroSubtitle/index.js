/**
 * HeroSubtitle Component
 * 
 * A professional hero subtitle component with:
 * - Mobile-first responsive typography
 * - Staggered animation system with customizable delays
 * - Full accessibility support with ARIA attributes
 * - PropTypes validation for type safety
 * - Performance optimizations with React.memo
 * - Support for multiple HTML elements (p, h2, h3, etc.)
 * - High contrast and reduced motion support
 * 
 * @see {@link ./HeroSubtitle.jsx} for component implementation
 * @see {@link ./HeroSubtitle.styled.js} for styling and animations
 */
export { default } from './HeroSubtitle';
